#
# Cassandra SideCar configuration file
#

cassandra:
  - host: localhost
  - port: 9042
#
# Configuration to enable SSL and authentication for the CQL connection
#
#  - ssl:
#      - enabled: true
#      - truststore:
#          - path: path/to/truststore.p12
#          - password: password
#
#  - username: cassandra
#  - password: cassandra


sidecar:
  - host: 0.0.0.0
  - port: 9043
#
# Enable SSL configuration (Disabled by default)
#
#  - ssl:
#      - enabled: true
#      - keystore:
#          - path: "path/to/keystore.p12"
#          - password: password
#      - truststore:
#          - path: "path/to/truststore.p12"
#          - password: password


healthcheck:
  - poll_freq_millis: 30000
